eccodes>=1.5,<2
numpy>=1.24,<3
anthropic>=0.40,<1
# Optional fallback for GRIB2 (used if eccodes Python bindings unavailable)
# cfgrib
# xarray
# Optional: Firebase Firestore for accuracy log persistence
# firebase-admin>=6.0,<7
