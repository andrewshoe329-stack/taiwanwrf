name = "himawari-proxy"
main = "src/worker.js"
compatibility_date = "2024-01-01"

# Free tier: 100k requests/day, 10ms CPU per request — more than enough
# Deploy: npx wrangler deploy
# Dev:    npx wrangler dev
